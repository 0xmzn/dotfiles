# i3blocks configuration file
#
# The i3blocks man page describes the usage of the binary,
# and its website describes the configuration:
#
#     https://vivien.github.io/i3blocks


# Global properties
separator=true
separator_block_width=15

[mediaplayer]
label=🎵 
command=/home/msaad/Scripts/i3blocks/mediaplayer
instance=spotify
interval=1
signal=10

[brigtness]
label=💡 
command=xbacklight | awk -F. '{print $1}'
interval=10

[memory]
label=👾 
command=free -h | awk '/^Mem:/ {print $3 " / " $2}'
interval=30

[Kernel]
label=🌽 
command=uname -r
interval=10800

[Packages]
label=📦 
command=pacman -Q | wc -l
interval=3600

[Volume]
label=🔉 
command=awk -F"[][]" '/dB/ { print $2 }' <(amixer sget Master)
interval=1

[battery]
command=/home/msaad/Scripts/i3blocks/battery.sh
interval=1

[time]
label=🕒 
command=date '+%a %m-%d %H:%M'
interval=60
